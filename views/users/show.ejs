<!DOCTYPE html>
<html>
  <head>
    <title>Usuários Detalhes</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <div class="container">
      <h3><a href="/users">Lista de Usuários</a></h3>
      <h1>Detalhes de Usuários</h1>
      <table border="1">
        <tbody>
          <tr>
            <td>Nome</td>
            <td><%= user.name %></td>
          </tr>
          <tr>
            <td>Usuário</td>
            <td><%= user.userid %></td>
          </tr>
          <tr>
            <td>Senha</td>
            <td><%= user.password %></td>
          </tr>
        </tbody>
      </table>
      <h3><a href="/users/edit/<%= user._id%>">Editar</a></h3>
      <form action="/users/delete/<%= user._id%>" method="post">
        <button type="submit">Excluir</button>
      </form>
    </div>
  </body>
</html>
